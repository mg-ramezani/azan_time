#ifndef TYPES_H
#define TYPES_H

#include <vector>
#include <string>

using vec_double = std::vector<double>;
using vec_string = std::vector<std::string>;
using vec_strings = std::vector<vec_string>;
using cordinate = std::pair<double, double>;

#endif // TYPES_H
